name: Auto Renew Katabump Server

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  renew:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Google Chrome
        run: |
          sudo apt-get update
          sudo apt-get install -y wget gnome-keyring
          wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo dpkg -i google-chrome-stable_current_amd64.deb || sudo apt-get install -f -y
          google-chrome --version

      # 【新增】安装 Xray 核心，用于将 VLESS 转换为本地 HTTP 代理
      - name: Install Xray Core
        run: |
          bash <(curl -L https://github.com/XTLS/Xray-install/raw/main/install-release.sh)
          xray --version

      - name: Install Node dependencies
        run: |
          npm install

      - name: Install Playwright Browsers
        run: |
          npx playwright install chromium
          npx playwright install-deps chromium

      - name: Create screenshots directory
        run: |
          mkdir -p screenshots

      - name: Run Renew Script
        env:
          USERS_JSON: ${{ secrets.USERS_JSON }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          # 此处直接填你的 vless:// 链接，或者填写链接对应的 Secret 名称
          HTTP_PROXY: ${{ secrets.HTTP_PROXY }}
          CHROME_PATH: '/usr/bin/google-chrome'
          NODE_ENV: 'production'
        run: |
          node action_renew.js

      - name: Upload screenshots artifact on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: failure-screenshots
          path: screenshots/
          retention-days: 7
